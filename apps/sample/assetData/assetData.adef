executables:
{
    assetData = ( assetData )
}

processes:
{
    run:
    {
        ( assetData )
    }
}

start: manual

bindings:
{
    assetData.assetData.le_avc -> avcService.le_avc
    assetData.assetData.le_avdata -> avcService.le_avdata
}
