start: manual

executables:
{
    secStoreTestGlobal = ( secStoreTestGlobal )
}

processes:
{
    run:
    {
        ( secStoreTestGlobal )
    }
}

bindings:
{
    secStoreTestGlobal.secStoreTestGlobal.secStoreGlobal -> secStore.secStoreGlobal
    secStoreTestGlobal.secStoreTestGlobal.secStoreAdmin -> secStore.secStoreAdmin
}
