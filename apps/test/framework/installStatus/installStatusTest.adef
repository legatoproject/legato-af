start: manual

executables:
{
    installStatusTest = (installNotify)
}

processes:
{
    run:
    {
        (installStatusTest)
    }
}

bindings:
{
    installStatusTest.installNotify.le_instStat -> <root>.le_instStat
}

