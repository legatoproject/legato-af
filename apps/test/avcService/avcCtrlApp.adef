
executables:
{
    avcCtrlApp = ( componentCtrlApp )
}

processes:
{
    run:
    {
        // Normally should be executed with 'app runProc' using:
        //     app runProc avcCtrlApp --exe=/bin/avcCtrlApp -- x
        // where x is the test case number.
        // (avcCtrlApp)
    }
}

bindings:
{
    avcCtrlApp.componentCtrlApp.le_avc -> avcService.le_avc
}
